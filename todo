time issues
	set timezone for all the time fields
	fix the f***** issue with 64 bit ns fields
filter out predictions made after closing
put NA everywhere where we actually don't know the answer instead of inventing something
sort question columns the same way as forecast columns
